# Icon source notes

The current app icon is generated entirely from vector drawing commands in
`scripts/AppIcon.swift`.

It is an original "Window + Container Core" composite mark. It does not embed
Apple's official `container` / Containerization logo bitmap.

The earlier vendored Apple logo bitmap was removed so the repository no longer
ships Apple's mark.
